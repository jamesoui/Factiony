# === CONFIGURATION FACTIONY - ARCHITECTURE POLYGLOTTE ===

# 📊 Supabase (PostgreSQL) - Données structurées et critiques
# Récupérez ces valeurs depuis votre dashboard Supabase
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# 🔥 Firebase Configuration - Données dynamiques et volumineuses
# IMPORTANT: Les variables VITE_* sont publiques côté client et ne doivent jamais contenir
# de secrets (service accounts, private keys). Ces valeurs sont safe pour le client.
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id

# 🎮 APIs Externes (optionnel)
# Pour récupérer les données des jeux depuis RAWG et IGDB
VITE_RAWG_API_KEY=your_rawg_api_key
VITE_IGDB_CLIENT_ID=your_igdb_client_id
VITE_IGDB_CLIENT_SECRET=your_igdb_client_secret

# 💳 Stripe (pour les paiements Premium)
# Clé publique Stripe pour les abonnements
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key

# === INSTRUCTIONS DE CONFIGURATION ===

# 1️⃣ SUPABASE:
#   - Créez un projet sur https://supabase.com
#   - Copiez l'URL et la clé anon depuis Settings > API
#   - Exécutez les migrations SQL dans l'éditeur SQL

# 2️⃣ FIREBASE/FIRESTORE:
#   - Créez un projet sur https://console.firebase.google.com
#   - Dans Project Settings > General, copiez vos identifiants Firebase
#   - Activez Firestore Database dans Build > Firestore Database
#   - IMPORTANT: N'utilisez JAMAIS de Service Account ou private keys côté client
#   - Les variables VITE_FIREBASE_* sont conçues pour être publiques

# 3️⃣ VÉRIFICATION:
#   - Démarrez l'app avec `npm run dev`
#   - Vérifiez la console pour les messages de connexion
#   - Toutes les bases doivent afficher "✅ Connecté"

# === TABLES SUPABASE REQUISES ===
# users (id, email, username, avatar_url, is_premium, is_private, created_at)
# subscriptions (id, user_id, plan, status, start_date, end_date, created_at)
# follows (id, user_id, friend_id, created_at)

# === COLLECTIONS FIRESTORE REQUISES ===
# user_likes (user_id, game_id, created_at)
# comments (user_id, game_id, content, rating, is_spoiler, likes, created_at)
# user_lists (user_id, name, games[], is_public, created_at)
# games_api_cache (game_id, api_source, data_json, expires_at)
# logs (user_id, action_type, resource_id, metadata, timestamp)